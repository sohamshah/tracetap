// tracetap observatory — chart library (inlined by composePage before app.js).
// Hand-rolled SVG, zero dependencies. Everything renders into a container
// element and returns it; tooltips go through the shared TT singleton.
"use strict";

/* Placeholder — populated in the viz wave. */
const TracetapCharts = {};
