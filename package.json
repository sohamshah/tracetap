{
  "name": "tracetap",
  "version": "0.2.0",
  "description": "Capture the full trajectory of a coding-agent harness (Claude Code, Codex) — request bodies, system prompts, tools, streamed responses, token usage — into a JSONL log and a self-contained HTML viewer, by proxying the agent's API traffic through a local HTTP server.",
  "main": "dist/tracetap.js",
  "bin": {
    "tracetap": "dist/tracetap.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/tracetap.js dist/claude-cli.js dist/codex-cli.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "files": [
    "dist/**/*",
    "frontend/dist/**/*",
    "frontend/template.html",
    "frontend/codex-template.html",
    "frontend/patches/**/*",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "tracetap",
    "trace",
    "trajectory",
    "claude",
    "claude-code",
    "anthropic",
    "codex",
    "openai",
    "agent",
    "coding-agent",
    "logging",
    "api",
    "proxy",
    "debugging",
    "observability"
  ],
  "license": "MIT",
  "author": "Soham Shah",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sohamshah/tracetap.git"
  },
  "homepage": "https://github.com/sohamshah/tracetap#readme",
  "bugs": {
    "url": "https://github.com/sohamshah/tracetap/issues"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
